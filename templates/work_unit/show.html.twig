{% extends 'base.html.twig' %}

{% block title %}WorkUnit{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>WorkUnit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work_unit.id }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ work_unit.start ? work_unit.start|date('Y-m-d H:i') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ work_unit.end ? work_unit.end|date('Y-m-d H:i') : '' }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('work_unit/_delete_form.html.twig') }}

    <ul>
        <li><a href="{{ path('app_work_unit_index') }}">back to list</a></li>
        <li><a href="{{ path('app_work_unit_edit', {'id': work_unit.id}) }}">edit</a></li>
    </ul>

</div>
{% endblock %}
