{% extends 'base.html.twig' %}

{% block title %}WorkUnit{% endblock %}

{% block body %}
    <h1>WorkUnit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work_unit.id }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ work_unit.start ? work_unit.start|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ work_unit.end ? work_unit.end|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_work_unit_index') }}">back to list</a>

    <a href="{{ path('app_work_unit_edit', {'id': work_unit.id}) }}">edit</a>

    {{ include('work_unit/_delete_form.html.twig') }}
{% endblock %}
