<form method="post" action="{{ path('app_work_unit_delete', {'id': work_unit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ work_unit.id) }}">
    <button class="btn">Delete</button>
</form>
